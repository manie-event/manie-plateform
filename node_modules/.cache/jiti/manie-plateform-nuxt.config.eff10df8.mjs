"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;var _kit = await jitiImport("@nuxt/kit");

const { resolve } = (0, _kit.createResolver)("file:///Users/clementmoreno/Documents/manie-plateform/nuxt.config.ts");

// https://nuxt.com/docs/api/configuration/nuxt-config
var _default = exports.default = defineNuxtConfig({
  // Enable server-side rendering
  ssr: false,
  runtimeConfig: {
    public: {
      tokenStripe: process.env.NUXT_PUBLIC_TOKEN_STRIPE,
      tokenSiret: process.env.TOKEN_SIRET,
      apiUrl: process.env.NUXT_API_URL || 'https://manie-api.onrender.com'
    }
  },
  // TypeScript configuration
  typescript: {
    shim: false
  },

  // Vuetify build configuration
  build: {
    transpile: ['vuetify']
  },

  // Modules
  modules: ['@pinia/nuxt'],

  // Application metadata
  app: {
    head: {
      title: 'Manie - Facilite vos événements'
    }
  },

  // Nitro configuration
  nitro: {
    serveStatic: true
  },

  // Dev server handlers
  devServerHandlers: [],

  compatibilityDate: '2024-09-06'
}); /* v9-d696d2ae32ff7bb0 */
