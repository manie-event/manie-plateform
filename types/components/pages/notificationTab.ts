import type { TablerIconComponent } from 'vue-tabler-icons';
interface notificationTabType {
  title: string;
  subtitle: string;
  icon: TablerIconComponent;
  switch: boolean;
}
export type { notificationTabType };
